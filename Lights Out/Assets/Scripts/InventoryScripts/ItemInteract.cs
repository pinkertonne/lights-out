using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class ItemInteract : MonoBehaviour
{
    /*
    [SerializeField]
    private GameObject playerCam;
    [SerializeField]
    private Text pressText;
    // Update is called once per frame
    void FixedUpdate()
    {
        RaycastHit hit = new RaycastHit();

        if (Physics.Raycast(playerCam.transform.position, playerCam.transform.TransformDirection(Vector3.forward), out hit, 4f, LayerMask.NameToLayer("Drawers"))) {
            pressText.gameObject.SetActive(true);
            if (Input.GetKeyDown(KeyCode.E))
            {
                hit.collider.transform.root.gameObject.GetComponent<Drawer>().ToggleDrawer();
            }
        } else
        {
            pressText.gameObject.SetActive(false);
        }
    }
    */
}
